import { StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    alignItems: 'center',
    marginTop: 20,
    marginHorizontal: 10,
    padding: 20,
    borderWidth: 2,
    borderColor: 'black',
    borderRadius: 10,
  },
});

export default styles;
